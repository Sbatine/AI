% Base case: anything raised to power 0 is 1
power(_, 0, 1).

% Recursive case: Num^Pow = Num * Num^(Pow-1)
power(Num, Pow, Ans) :-
    Pow > 0,
    P1 is Pow - 1,
    power(Num, P1, A1),
    Ans is Num * A1.

ex:
?- power(2, 3, X).

X = 8.


ex:
?- power(5, 0, X).
X = 1.